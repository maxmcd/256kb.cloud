version: 2
sql:
  - gen:
      go:
        package: db
        out: ./
        emit_prepared_queries: true
        emit_interface: true
    engine: sqlite
    schema: migration.sql
    queries: queries.sql
